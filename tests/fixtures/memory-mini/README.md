# memory-mini

Synthetic validation fixture for the Adaptive Memory Engine MVP pipeline.

The fixture follows `adaptive_memory_engine_validation_dataset_spec_v1.md` and covers Bronze ingest, deterministic Silver extraction, type-gate rejection, Gold graph/timeline generation, local query fallback, and Obsidian export.
